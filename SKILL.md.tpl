---
name: memory
description: 使用 mem.py 双层记忆系统（树状时间存档 + FTS5关键词+语义向量双通道检索 + 自动捕获 + MEMORY.md 常驻层）。当用户提到"记忆/回忆/上次说过/之前讨论/记录下来/别忘了"或需要跨会话引用历史对话、检索旧结论、删除某段记忆时使用。维护两层纪律：常驻层存高频事实，存档层存完整对话，检索永远先摘要后全文。
---

# 记忆系统 (mem.py)

记忆库为**全局库** {{MEMORY_HOME}}（默认 $DSH_HOME/memories，环境变量 MEMORY_HOME 可覆盖为项目级库）。入口脚本 {{MEM_PY}}（由 install.py 自动生成本文件，路径已适配本机）。全部命令通过 pwsh 执行：python {{MEM_PY}} <命令>。

## 两层纪律（务必遵守）

- **常驻层 MEMORY.md**（≤2200 字符）：只存高频事实——用户偏好、环境、常驻约定。会话开始时像系统提示词一样注入。满了就 mem rm 删旧条目或当场合并，不要塞细节。
- **存档层 notes/**：每次有信息量对话结束，用 add 写入。摘要必须结构化：结论、决定、待办、关键事实，供检索层定位。
- **检索永远从最便宜的层开始**：常驻层 → 摘要层 → 正文层。禁止把索引或全部摘要塞进上下文。

## 自动捕获（重要）

- **dsh-memory 插件会自动把每轮用户消息原文**追加到 notes/pending/日期.md（兜底层，防漏记）。
- **你的强制流程**：每次有信息量的对话结束（有结论/决定/偏好/约定产出），**必须主动调用 memory_add** 写入结构化摘要——不要等用户说"记一下"。这就是自动记忆：模型负责提炼，插件负责兜底。
- 缓冲整理：`python {{MEM_PY}} pending` 查看待整理原话，有价值的用 add 结构化入库。

## 常用命令

| 场景 | 命令 |
|---|---|
| 记录一次对话 | python {{MEM_PY}} add "标题" -t "标签1,标签2" -s "结构化摘要" --content "正文" |
| 追加同主题内容 | 加 --append（追加到同日期文件；跨日期用 --date YYYY-MM-DD HH:MM 指定） |
| 检索 | python {{MEM_PY}} search "关键词" -k 3（语义向量+关键词双通道，换种说法也能搜到） |
| 生成注入块 | python {{MEM_PY}} inject "关键词" -k 3（输出 memory 块，直接拼进回答上下文） |
| 读完整记忆 | python {{MEM_PY}} get "notes/YYYY/MM/DD-标题.md" |
| 按时间浏览 | python {{MEM_PY}} list [YYYY-MM] |
| 写常驻事实 | python {{MEM_PY}} mem add "事实文本"；查看 mem show；删除 mem rm "唯一子串" |
| 删除记忆 | python {{MEM_PY}} rm "路径"（进回收站，trash 查看、restore 文件名 恢复）；rm --hard 永久删除 |
| 重建索引 | 手工改过文件后运行 python {{MEM_PY}} index（重建 FTS5 + 重新生成 _index.md） |
| 月度聚合 | python {{MEM_PY}} rollup YYYY-MM，然后由模型把机械列表合并成 3-5 条粗粒度摘要写回该文件 |
| 待整理缓冲 | python {{MEM_PY}} pending（查看自动捕获的用户原话） |

## 触发时机

- **写入**：对话产出了结论、决定、偏好、环境事实、重要约定时，主动 add（摘要必须信息密度高，别写"用户问了X"这种废话）。高频事实同步进 mem add。
- **检索**：用户问题涉及"之前/上次/某天聊过"时，先 search 或 inject，命中则基于摘要回答，需要细节再 get 全文。
- **删除**：用户要求忘记/删除某段记忆时 rm（软删除可反悔）；要求彻底遗忘时 rm --hard。

## 摘要模板（写入时套用）

-s "结论: <结论>; 决定: <决定>; 待办: <待办>; 关键事实: <事实>"

## Token 纪律

单次注入 ≤ 3 条检索结果（约 2k token）；MEMORY.md 全程 ≤ 2200 字符。存储与检索本身 0 token，只有注入才花钱。语义检索依赖本地 fastembed（可选）：未安装自动退回关键词检索。